const editImages = (prompt, UID, image) => {
    //TODO:
    // Declare the firebase function, get the url and follow the same process as the generateImages function.
    // Merge editImages and generateImages into one file and export both functions.
}

export { editImages };